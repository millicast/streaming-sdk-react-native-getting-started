import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    borderRadius: 6,
    height: 40,
    width: 160,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
